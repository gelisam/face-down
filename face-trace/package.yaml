name: face-trace

dependencies:
  - JuicyPixels
  - async
  - base
  - containers
  - ffmpeg-light
  - gloss
  - gloss-juicy
  - lens
  - stm

ghc-options:
  - -Wall

library:
  source-dirs: src

executables:
  face-trace:
    main: main.hs
    source-dirs: app
    dependencies:
      - face-trace
      - optparse-generic
